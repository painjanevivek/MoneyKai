# The frontend deploy is a static Expo export. Runtime feature APIs live on the
# MoneyKai backend, so do not package duplicate v1 API entrypoints here. Google
# OAuth fallback routes are dispatched by api/auth-google.js.
api/v1/**

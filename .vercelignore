# The frontend deploy is a static Expo export. Runtime feature APIs, including
# Google OAuth, live on the canonical private FastAPI backend. Do not package
# duplicate v1 API entrypoints in the web deployment.
api/v1/**

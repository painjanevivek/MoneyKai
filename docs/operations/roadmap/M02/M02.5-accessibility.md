# M02.5 Evidence: Rendered Accessibility and Legibility

## Identity

| Field | Value |
|---|---|
| Task | `M02.5` |
| Outcome | Validate WCAG AA, assistive navigation, 200% text, reduced motion, targets, and glass legibility |
| Status | Conditional pass — physical TalkBack confirmation pending |
| Branch | `codex/m02-design-system` |
| Base task revision | `f0c1878` |
| Task revision | The focused commits containing this record and final physical-device evidence |
| Captured | `2026-09-21` |

## Automated Results

| Check | Result |
|---|---|
| Accessibility/token contracts | Pass; 4 files and 23/23 focused tests |
| Complete mobile regression suite | Pass; 27 files and 198/198 tests through `npm run mobile:test` |
| WCAG AA token pairs | Pass for primary text, secondary text, action text, and glass-selection text |
| Touch foundations | Pass; token minimum 44 dp |
| Active font scaling restrictions | Pass; no disabled scaling and tab labels allow 200% |
| Reduced motion | Pass; runtime accessibility preference drives 0 ms component and navigation transitions |
| Mobile TypeScript and ESLint | Pass |
| Android Lint | Pass; 0 errors, 33 non-blocking warnings |
| Native compile | Pass; Android debug APK produced |

Android Lint initially exposed eight release-blocking native defects. The task moved the API-33
splash behavior into a version-qualified resource, corrected persisted picker permission flags, and
declared telephony hardware optional for devices without it. The rerun completed with zero errors.
The complete-suite runner now resolves contract fixtures relative to each test file and isolates
pure analytics tests from React Native persistence, so it passes from the canonical package script.

## Rendered API 36 Results

The APK was installed on `MoneyKai_API_36` (`emulator-5554`, 1080 × 1920, density 420). Metro
bundled the monorepo successfully after the mobile resolver received an explicit local-domain
mapping. Demo-mode email sign-in opened the authenticated dashboard.

| Rendered check | Result |
|---|---|
| Default login and logo placement | Pass; aligned 64 dp mark and no missing icon glyphs |
| Progressive email reveal | Pass; label, hint, password visibility, recovery, and sign-in actions exposed |
| Dashboard hierarchy | Pass; amount, income, spend, budget consequence, and recent activity visible |
| Glass navigation | Pass; five ordered tabs, 2 px gap, selected state, solid readable fallback |
| 200% login text | Pass; content reflows within a vertical ScrollView |
| 200% dashboard/tab text | Pass; labels remain readable and the tab bar supports two-line Transactions |
| Target geometry | Pass; 3–10 clickable nodes per capture, minimum rendered height 43.8 dp from 44 dp rounding |
| Hardware-keyboard focus | Pass; Tab focus reached the visible Review action with focusable/clickable state |
| Zero-animation device preference | Pass; window, transition, and animator scales all set to `0` |
| Last-action clearance | Pass after reserving tab-bar height plus 40 dp in every primary screen ScrollView |

## Durable Artifacts

- `login-default.png` / `login-default.xml`
- `login-expanded.png` / `login-expanded.xml`
- `login-font-200.png` / `login-font-200.xml`
- `dashboard-default.png` / `dashboard-default.xml`
- `dashboard-font-200.png` / `dashboard-font-200.xml`
- `dashboard-focus.xml`
- `more-default.png` / `more-default.xml`

Verified debug APK: 132,796,314 bytes; SHA-256
`BD2941C66721D3D42DE7BEE879E082C9DC3D6DABE36C8B0A0F076AB830235F6A`.

## Remaining Acceptance Item

The supplied physical wireless-debugging endpoint `192.168.1.5:40339` actively refused the
connection and ADB mDNS discovered no replacement. M02.5 and the M02 gate remain open until the
same APK is installed on the supported physical Android phone and the operator confirms TalkBack
order/announcements, disclosure expanded state, reduced motion, 200% text scrolling, and dock
legibility. No emulator result is represented as physical-device evidence.

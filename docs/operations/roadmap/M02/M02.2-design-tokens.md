# M02.2 Evidence: Production Design Tokens

## Identity

| Field | Value |
|---|---|
| Task | `M02.2` |
| Outcome | Establish solid color, type, spacing, shape, elevation, icon, and motion tokens with no gradient or runtime dark path |
| Status | Pass |
| Branch | `codex/m02-design-system` |
| Base task revision | `247dae04606b2ff23a2dd9af35b6f8517b4fb593` |
| Task revision | The focused commit containing this record |
| Captured | `2026-09-21` |
| Owner / approver | Mobile / design system |

## Delivered Outcome

- Added one centralized [mobile design-system contract](../../../design/mobile-design-system.md).
- Added typed brand, semantic color, typography, spacing, shape, control, icon, motion, elevation,
  and restrained-glass tokens.
- Replaced the production light scheme with the approved lime, olive, cream, warm off-white, and
  near-black system.
- Switched the active runtime typography to installed system families instead of an unloaded font.
- Forced the active and alternate app shells, navigation themes, auth shell, and status bars to the
  light production appearance.
- Removed theme/dark controls from both Settings implementations.
- Normalized legacy stored/backup theme choices to light without making older data unreadable.
- Added four focused token-contract tests.

## Validation

```powershell
npm ci --workspaces --include-workspace-root
npx vitest run apps/MoneyKai-mobile/src/constants/designTokens.test.ts --pool=threads --maxWorkers=1
npm run mobile:typecheck
npm run mobile:lint
rg -n "Dark mode|Dark Mode|LinearGradient|expo-linear-gradient|setDarkModeEnabled|darkModeEnabled" \
  apps/MoneyKai-mobile/App.tsx \
  apps/MoneyKai-mobile/src/navigation \
  apps/MoneyKai-mobile/src/screens \
  apps/MoneyKai-mobile/src/components \
  apps/MoneyKai-mobile/src/hooks
git diff --check
```

## Results

| Check | Result |
|---|---|
| Locked dependency install | Pass; 1,427 packages installed from the committed lockfile |
| Token tests | Pass; 1 file, 4/4 tests |
| AA token pairs | Pass; text/canvas, secondary/surface, primary/surface, and ink/lime action pairs |
| Light-only normalization | Pass; legacy palette and dark values resolve to the light theme |
| Touch/type/motion foundations | Pass; 44 px target, 12 px minimum token, 0 ms reduced motion |
| Mobile TypeScript | Pass |
| Mobile ESLint | Pass |
| Reachable dark/gradient UI scan | Pass after removing the final Settings/More appearance copy |
| Whitespace | Pass |

## Residual Risk

The current component library still contains duplicated styling, constant-false compatibility
branches, and unverified rendered states. M02.3 migrates reusable primitives and owns the reduced-
motion runtime hook. M02.4 applies them to V1 journeys. M02.5 supplies rendered, screen-reader,
font-scale, focus, touch-target, physical-device, and glass-fallback evidence.

The dependency install reports 7 high, 31 moderate, and 1 low advisory before policy filtering.
M07 owns remediation and M00's always-on dependency policy remains the release authority.

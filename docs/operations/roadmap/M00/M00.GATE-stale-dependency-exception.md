# M00 Gate Remediation: Stale Dependency Exception

## Supersession Note — 2026-09-21

The conclusion below was valid for the recorded Windows graph but not portable to the clean
Linux CI graph, which continued to report `@expo/xcpretty`. The package remains a named,
time-bounded exception under the stricter cross-environment policy documented in
[the follow-up evidence](./M00.GATE-cross-environment-audit-policy.md). This note preserves
the original finding while preventing it from being read as a repository-wide resolution.

## Record

| Field | Value |
|---|---|
| Outcome | Remove the stale `@expo/xcpretty` audit exception detected by the M00 gate |
| Status | Pass |
| Environment | Isolated worktree `codex/m00-governance` on Windows |
| Base revision | `713be2d1eee0ca868cc2c2170c217e65a64ace2e` |
| Fix revision | The commit containing this evidence record |
| Captured | `2026-09-21T17:51:49.7751560+05:30` |

## Finding and Correction

`npm run security:audit-dependencies` failed closed because `@expo/xcpretty` was listed in
`PR1-EXPO56-TRANSITIVE-JSON-TOOLS` but was no longer reported by the current npm advisory
graph. Retaining the entry could conceal a future unrelated advisory under a stale approval.

The correction removes only `@expo/xcpretty` from that exception. It does not remove the
package from the lockfile, alter runtime/build versions, or expand the exception. The three
still-reported packages remain covered only until the existing 2026-09-24 review deadline.

## Validation

```powershell
npm run security:audit-dependencies
npm run security:audit-dependencies:test
git diff --check
```

Pass conditions:

- the policy audit exits zero with no stale or unapproved finding;
- dependency-audit regression tests pass;
- the audit continues to report seven high build-tool findings and their current,
  time-bounded exceptions rather than describing the dependency graph as clean; and
- the diff is limited to the narrowed exception and this evidence record.

## Result

All pass conditions succeeded. No dependency, lockfile, runtime, or application behaviour
changed. The remaining high findings and September 24 exception expiry stay active risks.
